import 'package:flutter/material.dart';

class Tiles extends StatelessWidget {
  final List<Widget> tiles;

  const Tiles({Key key, this.tiles}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row();
  }
}
