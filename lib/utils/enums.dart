enum TargetType { User, Subreddit }
enum SupportedSortTypes { hot, best, top, newest, controversial }
enum PostRange { hour, day, week, month, year, all }
enum BottomScreens { post, search, home, inbox, profile }
enum AppThemes { light, dark }
enum InboxType { ALL, UNREAD, SENT }
enum ProfileTabs { OVERVIEW, ABOUT, POSTS, COMMENTS, SAVED, HIDDEN, GILDED }
enum HistoryType { subreddit, submission }
enum PreferenceType { Bool, Int, Double, String, StringList }
